<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Acción de servidor -->
        <record id="action_buscar_coincidencias_api" model="ir.actions.server">
            <field name="name">Buscar Coincidencias de Productos</field>
            <field name="model_id" ref="stock.model_stock_picking_type"/>
            <field name="binding_model_id" ref="stock.model_stock_picking_type"/>
            <field name="state">code</field>
            <field name="code">
                action = model.importar_precios_desde_api()
            </field>
        </record>

        <!-- Menú en Inventario -->
        <menuitem id="menu_buscar_coincidencias"
                  name="Buscar Coincidencias API"
                  parent="stock.menu_stock_root"
                  action="action_buscar_coincidencias_api"
                  sequence="110"/>
    </data>
</odoo>